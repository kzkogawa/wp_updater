<a class="small" href="${postSourceUrl}" target="_brank" rel="nofollow">${postTitle}</a>
<div style="text-align: center">
	<#list videoUrls as item><a class="small" href="${item}" target="_brank" rel="nofollow">${item_index + 1}</a></#list>
</div>
